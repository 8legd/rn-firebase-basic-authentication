/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 */

import React, {
  AppRegistry,
  Component,
  StyleSheet,
  Text,
  View
} from 'react-native'
import Router from './Router'

AppRegistry.registerComponent('rn_firebase', () => Router);
